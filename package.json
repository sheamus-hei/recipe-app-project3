
{
  "name": "noscroll",
  "version": "1.0.0",
  "description": "Project3 SEI 29",
  "main": "server/server.js",
  "engines": {
    "node": "v13.7.0"
  },
  "author": "Erik, John, Mac",
  "license": "ISC",
  "scripts": {
    "predeploy": "cd client && npm run build",
"deploy": "gh-pages -b gh-pages -d client/build",
      "server-postinstall": "cd server && npm install",
      "server-start": "node server/server.js",
      "server-build": "cd server && npm run build"
    },
    "devDependencies": {
      "npm-run-all": "^4.0.2"
    }
  }